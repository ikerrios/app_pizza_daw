services:
  db:
    image: postgres:16
    container_name: postgres_pizza
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: order_app
    ports:
      - "5432:5432"